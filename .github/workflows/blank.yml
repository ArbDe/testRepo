name: "Delete old folders from S3"
on:
  push:
  workflow_dispatch:
  
permissions:
  id-token: write


jobs:
  remove:
    runs-on: ubuntu-latest

    steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
       aws-access-key-id: AKIARWBN6WILAX7UHBP6
       aws-secret-access-key: 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
       aws-region: us-east-1

    - name: List S3 bucket folders
      run: |
            folders=$(aws s3 ls s3://awsbucketbeni/testFolder/ --recursive | awk '{print $2}' | xargs -n 1 basename)
            echo $folders


            for folder in $folders; do
              echo $folder
            done
