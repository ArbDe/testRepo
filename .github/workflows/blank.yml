name: "Delete old folders from S3"
on:
  push:
  workflow_dispatch:
  
permissions:
  id-token: write


jobs:
  remove:
    runs-on: ubuntu-latest

    steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
       aws-access-key-id: AKIA6BIPVMUD24P2CY6F
       aws-secret-access-key: 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
       aws-region: us-east-1

    - name: List S3 bucket folders
      run: |
            now=$(date +%s)
            cutOff=$(date -d '-30 minutes' +%s)
            folders=$(aws s3api list-objects --bucket beni-bucket --prefix testFolder/ )
            #for folder in $folders; do
             # echo $folder
            #done
              echo $folders









