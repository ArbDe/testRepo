name: "Delete old folders from S3"
on:
  push:
  workflow_dispatch:
  
permissions:
  id-token: write


jobs:
  remove:
    runs-on: ubuntu-latest

    steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
       aws-access-key-id: AKIAYJLMCLOUOEFGMSF3
       aws-secret-access-key: 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
       aws-region: us-east-1

    - name: List S3 bucket folders
      run: |
            now=$(date +%s)
            cutOff=$(date -d '-30 minutes' +%s)
            echo $cutOff
            folders=$(aws s3 ls s3://beni-bucket1/testFolder/ --recursive | sort | awk '{print $1 ","}')
            #for folder in $folders; do
            IFS=',' read -r -a array <<< "$folders"  

            # done
            echo $array
            echo $array[@]
            for i in "${array[@]}"
            do
              echo $i
              
            done
            









