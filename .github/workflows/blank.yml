name: "Delete old folders from S3"
on:
  push:
  workflow_dispatch:
  
permissions:
  id-token: write


jobs:
  remove:
    runs-on: ubuntu-latest

    steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
       aws-access-key-id: AKIARHOCZXC3Y6QUXKBH
       aws-secret-access-key: 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
       aws-region: us-east-1

    - name: List S3 bucket folders
      run: |
            now=$(date +%s)
            cutOff=$(date -d '-30 minutes' +%s)
            echo $cutOff
            folders=$(aws s3 ls s3://beni-bucket1/testFolder/ --recursive | sort | awk '{print $1}')
            
            arr=()
            
            echo $folders
            for folder in $folders; do
              arr+=($folder)
              #if [$folder == '2023-03-31']
              #then
                echo $folder
              #fi
            done
            
            echo ${$arr[@]}
            
       
            













